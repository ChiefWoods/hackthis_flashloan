/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import { findVaultPda } from "../pdas";
import { FLASHLOAN_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const FLASH_LOAN_DISCRIMINATOR = new Uint8Array([
  239, 246, 59, 224, 139, 20, 175, 14,
]);

export function getFlashLoanDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(FLASH_LOAN_DISCRIMINATOR);
}

export type FlashLoanInstruction<
  TProgram extends string = typeof FLASHLOAN_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountBorrower extends string | AccountMeta<string> = string,
  TAccountInstructions extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountBorrower extends string
        ? WritableAccount<TAccountBorrower>
        : TAccountBorrower,
      TAccountInstructions extends string
        ? ReadonlyAccount<TAccountInstructions>
        : TAccountInstructions,
      ...TRemainingAccounts,
    ]
  >;

export type FlashLoanInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type FlashLoanInstructionDataArgs = { amount: number | bigint };

export function getFlashLoanInstructionDataEncoder(): FixedSizeEncoder<FlashLoanInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FLASH_LOAN_DISCRIMINATOR }),
  );
}

export function getFlashLoanInstructionDataDecoder(): FixedSizeDecoder<FlashLoanInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getFlashLoanInstructionDataCodec(): FixedSizeCodec<
  FlashLoanInstructionDataArgs,
  FlashLoanInstructionData
> {
  return combineCodec(
    getFlashLoanInstructionDataEncoder(),
    getFlashLoanInstructionDataDecoder(),
  );
}

export type FlashLoanAsyncInput<
  TAccountVault extends string = string,
  TAccountBorrower extends string = string,
  TAccountInstructions extends string = string,
> = {
  vault?: Address<TAccountVault>;
  borrower: Address<TAccountBorrower>;
  instructions?: Address<TAccountInstructions>;
  amount: FlashLoanInstructionDataArgs["amount"];
};

export async function getFlashLoanInstructionAsync<
  TAccountVault extends string,
  TAccountBorrower extends string,
  TAccountInstructions extends string,
  TProgramAddress extends Address = typeof FLASHLOAN_PROGRAM_ADDRESS,
>(
  input: FlashLoanAsyncInput<
    TAccountVault,
    TAccountBorrower,
    TAccountInstructions
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  FlashLoanInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountBorrower,
    TAccountInstructions
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLASHLOAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    borrower: { value: input.borrower ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda();
  }
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.borrower),
      getAccountMeta(accounts.instructions),
    ],
    data: getFlashLoanInstructionDataEncoder().encode(
      args as FlashLoanInstructionDataArgs,
    ),
    programAddress,
  } as FlashLoanInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountBorrower,
    TAccountInstructions
  >);
}

export type FlashLoanInput<
  TAccountVault extends string = string,
  TAccountBorrower extends string = string,
  TAccountInstructions extends string = string,
> = {
  vault: Address<TAccountVault>;
  borrower: Address<TAccountBorrower>;
  instructions?: Address<TAccountInstructions>;
  amount: FlashLoanInstructionDataArgs["amount"];
};

export function getFlashLoanInstruction<
  TAccountVault extends string,
  TAccountBorrower extends string,
  TAccountInstructions extends string,
  TProgramAddress extends Address = typeof FLASHLOAN_PROGRAM_ADDRESS,
>(
  input: FlashLoanInput<TAccountVault, TAccountBorrower, TAccountInstructions>,
  config?: { programAddress?: TProgramAddress },
): FlashLoanInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountBorrower,
  TAccountInstructions
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLASHLOAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    borrower: { value: input.borrower ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.borrower),
      getAccountMeta(accounts.instructions),
    ],
    data: getFlashLoanInstructionDataEncoder().encode(
      args as FlashLoanInstructionDataArgs,
    ),
    programAddress,
  } as FlashLoanInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountBorrower,
    TAccountInstructions
  >);
}

export type ParsedFlashLoanInstruction<
  TProgram extends string = typeof FLASHLOAN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    borrower: TAccountMetas[1];
    instructions: TAccountMetas[2];
  };
  data: FlashLoanInstructionData;
};

export function parseFlashLoanInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFlashLoanInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      borrower: getNextAccount(),
      instructions: getNextAccount(),
    },
    data: getFlashLoanInstructionDataDecoder().decode(instruction.data),
  };
}
