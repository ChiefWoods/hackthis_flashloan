/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findVaultPda } from "../pdas";
import { FLASHLOAN_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const FLASH_REPAY_DISCRIMINATOR = new Uint8Array([
  182, 143, 19, 23, 39, 221, 184, 78,
]);

export function getFlashRepayDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(FLASH_REPAY_DISCRIMINATOR);
}

export type FlashRepayInstruction<
  TProgram extends string = typeof FLASHLOAN_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountBorrower extends string | AccountMeta<string> = string,
  TAccountInstructions extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountBorrower extends string
        ? WritableSignerAccount<TAccountBorrower> &
            AccountSignerMeta<TAccountBorrower>
        : TAccountBorrower,
      TAccountInstructions extends string
        ? ReadonlyAccount<TAccountInstructions>
        : TAccountInstructions,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FlashRepayInstructionData = { discriminator: ReadonlyUint8Array };

export type FlashRepayInstructionDataArgs = {};

export function getFlashRepayInstructionDataEncoder(): FixedSizeEncoder<FlashRepayInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: FLASH_REPAY_DISCRIMINATOR }),
  );
}

export function getFlashRepayInstructionDataDecoder(): FixedSizeDecoder<FlashRepayInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFlashRepayInstructionDataCodec(): FixedSizeCodec<
  FlashRepayInstructionDataArgs,
  FlashRepayInstructionData
> {
  return combineCodec(
    getFlashRepayInstructionDataEncoder(),
    getFlashRepayInstructionDataDecoder(),
  );
}

export type FlashRepayAsyncInput<
  TAccountVault extends string = string,
  TAccountBorrower extends string = string,
  TAccountInstructions extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  vault?: Address<TAccountVault>;
  borrower: TransactionSigner<TAccountBorrower>;
  instructions?: Address<TAccountInstructions>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getFlashRepayInstructionAsync<
  TAccountVault extends string,
  TAccountBorrower extends string,
  TAccountInstructions extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FLASHLOAN_PROGRAM_ADDRESS,
>(
  input: FlashRepayAsyncInput<
    TAccountVault,
    TAccountBorrower,
    TAccountInstructions,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  FlashRepayInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountBorrower,
    TAccountInstructions,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLASHLOAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    borrower: { value: input.borrower ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda();
  }
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.borrower),
      getAccountMeta(accounts.instructions),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFlashRepayInstructionDataEncoder().encode({}),
    programAddress,
  } as FlashRepayInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountBorrower,
    TAccountInstructions,
    TAccountSystemProgram
  >);
}

export type FlashRepayInput<
  TAccountVault extends string = string,
  TAccountBorrower extends string = string,
  TAccountInstructions extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  vault: Address<TAccountVault>;
  borrower: TransactionSigner<TAccountBorrower>;
  instructions?: Address<TAccountInstructions>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getFlashRepayInstruction<
  TAccountVault extends string,
  TAccountBorrower extends string,
  TAccountInstructions extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FLASHLOAN_PROGRAM_ADDRESS,
>(
  input: FlashRepayInput<
    TAccountVault,
    TAccountBorrower,
    TAccountInstructions,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): FlashRepayInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountBorrower,
  TAccountInstructions,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLASHLOAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    borrower: { value: input.borrower ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.borrower),
      getAccountMeta(accounts.instructions),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFlashRepayInstructionDataEncoder().encode({}),
    programAddress,
  } as FlashRepayInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountBorrower,
    TAccountInstructions,
    TAccountSystemProgram
  >);
}

export type ParsedFlashRepayInstruction<
  TProgram extends string = typeof FLASHLOAN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    borrower: TAccountMetas[1];
    instructions: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: FlashRepayInstructionData;
};

export function parseFlashRepayInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFlashRepayInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      borrower: getNextAccount(),
      instructions: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getFlashRepayInstructionDataDecoder().decode(instruction.data),
  };
}
