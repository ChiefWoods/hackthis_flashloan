/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import { FLASHLOAN_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const LOG_BALANCE_DISCRIMINATOR = new Uint8Array([
  242, 234, 179, 241, 27, 60, 111, 93,
]);

export function getLogBalanceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(LOG_BALANCE_DISCRIMINATOR);
}

export type LogBalanceInstruction<
  TProgram extends string = typeof FLASHLOAN_PROGRAM_ADDRESS,
  TAccountAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAccount extends string
        ? ReadonlyAccount<TAccountAccount>
        : TAccountAccount,
      ...TRemainingAccounts,
    ]
  >;

export type LogBalanceInstructionData = { discriminator: ReadonlyUint8Array };

export type LogBalanceInstructionDataArgs = {};

export function getLogBalanceInstructionDataEncoder(): FixedSizeEncoder<LogBalanceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: LOG_BALANCE_DISCRIMINATOR }),
  );
}

export function getLogBalanceInstructionDataDecoder(): FixedSizeDecoder<LogBalanceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getLogBalanceInstructionDataCodec(): FixedSizeCodec<
  LogBalanceInstructionDataArgs,
  LogBalanceInstructionData
> {
  return combineCodec(
    getLogBalanceInstructionDataEncoder(),
    getLogBalanceInstructionDataDecoder(),
  );
}

export type LogBalanceInput<TAccountAccount extends string = string> = {
  account: Address<TAccountAccount>;
};

export function getLogBalanceInstruction<
  TAccountAccount extends string,
  TProgramAddress extends Address = typeof FLASHLOAN_PROGRAM_ADDRESS,
>(
  input: LogBalanceInput<TAccountAccount>,
  config?: { programAddress?: TProgramAddress },
): LogBalanceInstruction<TProgramAddress, TAccountAccount> {
  // Program address.
  const programAddress = config?.programAddress ?? FLASHLOAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    account: { value: input.account ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.account)],
    data: getLogBalanceInstructionDataEncoder().encode({}),
    programAddress,
  } as LogBalanceInstruction<TProgramAddress, TAccountAccount>);
}

export type ParsedLogBalanceInstruction<
  TProgram extends string = typeof FLASHLOAN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    account: TAccountMetas[0];
  };
  data: LogBalanceInstructionData;
};

export function parseLogBalanceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedLogBalanceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { account: getNextAccount() },
    data: getLogBalanceInstructionDataDecoder().decode(instruction.data),
  };
}
